---
docker_group: docker
docker_allow_users: []      # Ensure users exist
docker_opts: []
docker_http_proxy:
docker_tmp_dir:
docker_binary:

docker_api_version: '1.1.0'

docker_compose_version: '1.4.2'
docker_compose_binary: /usr/local/bin/docker-compose
